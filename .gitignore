# Repo-wide ignores. Per-project rules live in mobile/.gitignore and (later)
# backend/.gitignore.

# secrets — unanchored so they match at any depth
.env
.env.*
!.env.example

# OS
.DS_Store

# editors
.idea/
.vscode/
*.swp
